const id = "zenuml";
const detector = (txt) => {
  return /^\s*zenuml/.test(txt);
};
const loader = async () => {
  const { diagram } = await import("./zenuml-definition-0bd2d146.js").then((n) => n.z);
  return { id, diagram };
};
const plugin = {
  id,
  detector,
  loader
};
export {
  plugin as default
};
